name: Publish Resume from Remote

on:
  workflow_dispatch: {}
  # optional: run nightly
  schedule:
    - cron: '0 6 * * *'

permissions:
  contents: read

jobs:
  fetch-and-upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Prepare service account
        env:
          FIREBASE_SA: ${{ secrets.FIREBASE_SA }}
        run: |
          if [ -z "$FIREBASE_SA" ]; then
            echo "FIREBASE_SA secret is not set"; exit 1
          fi
          echo "$FIREBASE_SA" > serviceAccount.json
          chmod 600 serviceAccount.json

      - name: Set env
        run: |
          echo "FIREBASE_PROJECT_ID=${{ secrets.FIREBASE_PROJECT_ID }}" >> $GITHUB_ENV

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
  run: npm install

      - name: Run resume uploader (remote or local)
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ github.workspace }}/serviceAccount.json
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          REMOTE_GITHUB_TOKEN: ${{ secrets.REMOTE_GITHUB_TOKEN }}
          REMOTE_TEX_REPO: ${{ secrets.REMOTE_TEX_REPO }}
          REMOTE_TEX_PATH: ${{ secrets.REMOTE_TEX_PATH }}
          REMOTE_TEX_BRANCH: ${{ secrets.REMOTE_TEX_BRANCH }}
        run: npm run upload:local

      - name: Cleanup
        if: always()
        run: rm -f serviceAccount.json
